import * as wework from "./channels/wework.js";import * as feishu from "./channels/feishu.js";import * as dingtalk from "./channels/dingtalk.js";import * as telegram from "./channels/telegram.js";import * as bark from "./channels/bark.js";import * as ntfy from "./channels/ntfy.js";import * as slack from "./channels/slack.js";import * as generic from "./channels/generic-webhook.js";import {classifyDeliveryFailure} from "./retry.js";
export const defaultAdapters={wework,feishu,dingtalk,telegram,bark,ntfy,slack,"generic-webhook":generic};
export async function reconcileTargets(store,targets,nowMs){const active=new Map(targets.map(t=>[t.id,t]));for(const t of targets){const old=await store.getTargetRecord(t.id);if(!old||old.status!=="active")await store.putTargetRecord(t.id,{id:t.id,channel:t.channel,status:"active",enabledAt:old?.enabledAt??nowMs,disabledAt:null});}for(const old of await store.listTargetRecords()){if(old?.status==="active"&&!active.has(old.id)){await store.putTargetRecord(old.id,{...old,status:"disabled",disabledAt:nowMs});for(const s of await store.listSignals()){if(!(s.targetIds??[]).includes(old.id))continue;const d=await store.getDelivery(s.signalId,old.id);if(!d||d.status==="retry_wait")await store.putDelivery(s.signalId,old.id,{status:"disabled",attemptCount:d?.attemptCount??0,lastAttemptAt:nowMs});}}}return active;}
export async function loadEligibleBacklog(store,nowMs){const out=[];for(const s of await store.listSignals())for(const tid of s.targetIds??[]){const d=await store.getDelivery(s.signalId,tid);if(!d||d.status==="retry_wait"&&Number(d.nextAttemptAt??0)<=nowMs)out.push({signal:s,targetId:tid});}return out;}
export async function dispatchDeliveries({work,targetsById,store,adapters=defaultAdapters,attemptBudget=10,concurrency=3,nowMs=Date.now(),fetchFn=fetch}){const streams=new Map();for(const w of work){if(!streams.has(w.targetId))streams.set(w.targetId,[]);streams.get(w.targetId).push(w);}for(const arr of streams.values())arr.sort((a,b)=>String(a.signal.sortAt).localeCompare(String(b.signal.sortAt))||a.signal.signalId.localeCompare(b.signal.signalId));const queue=[...streams.entries()];let attempts=0;const results=[];async function run(){while(queue.length){const [tid,items]=queue.shift();for(const item of items){if(attempts>=attemptBudget)return;const target=targetsById.get(tid);if(!target){const d=await store.getDelivery(item.signal.signalId,tid);if(!d||d.status==="retry_wait")await store.putDelivery(item.signal.signalId,tid,{status:"disabled",attemptCount:d?.attemptCount??0,lastAttemptAt:nowMs});continue;}const prior=await store.getDelivery(item.signal.signalId,tid);if(prior?.status==="sent"||prior?.status==="permanent_failure"||prior?.status==="disabled"||prior?.status==="retry_wait"&&prior.nextAttemptAt>nowMs)continue;attempts++;const adapter=adapters[target.channel];const result=adapter?await adapter.send(target,{...item.signal.notification,signalId:item.signal.signalId},{fetchFn}):{ok:false,retryable:false,code:"unsupported_channel"};if(result.ok)await store.putDelivery(item.signal.signalId,tid,{status:"sent",attemptCount:(prior?.attemptCount??0)+1,sentAt:nowMs,lastAttemptAt:nowMs});else await store.putDelivery(item.signal.signalId,tid,classifyDeliveryFailure(result,(prior?.attemptCount??0)+1,nowMs));results.push({signalId:item.signal.signalId,targetId:tid,ok:result.ok});}}}await Promise.all(Array.from({length:Math.min(concurrency,queue.length||1)},()=>run()));return{attempts,results};}
