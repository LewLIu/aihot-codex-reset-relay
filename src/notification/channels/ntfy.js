import { neutralizeMentions, truncateText } from "../../utils/text.js";
import { httpFailure, timeoutSignal, retryAfterMs } from "./_shared.js";
export function encodeRfc2047(value){const text=String(value??"");if(/^[\x00-\x7F]*$/.test(text))return text;const bytes=new TextEncoder().encode(text);let binary="";for(const b of bytes)binary+=String.fromCharCode(b);return `=?UTF-8?B?${btoa(binary)}?=`;}
export async function send(target,note,{fetchFn=fetch}={}){const server=String(target.config.server??"https://ntfy.sh").replace(/\/+$/,""),url=`${server}/${encodeURIComponent(target.config.topic)}`;const headers=new Headers({"Content-Type":"text/plain; charset=utf-8","Title":encodeRfc2047(truncateText(neutralizeMentions(note.title??"Codex Reset"),200))});if(target.config.token)headers.set("Authorization",`Bearer ${target.config.token}`);try{const r=await fetchFn(url,{method:"POST",headers,body:truncateText(neutralizeMentions(note.content??""),4000),signal:timeoutSignal()});return r.ok?{ok:true}:{...httpFailure(r.status),retryAfterMs:retryAfterMs(r)};}catch(e){return{ok:false,retryable:true,code:e?.name==="TimeoutError"?"timeout":"network"};}}
